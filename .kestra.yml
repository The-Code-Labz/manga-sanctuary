# Kestra deployment configuration
# env_prefix: Infisical secret prefix for this app's environment variables
# app_slug: identifier used for function naming and registry
env_prefix: MANGA_SANCTUARY
app_slug: manga-sanctuary
